<p id="notice"><%= notice %></p>

<p>
  <strong>Start:</strong>
  <%= @show.start.strftime("%a %d %b %l:%M %p") %>
</p>

<p>
  <strong>End:</strong>
  <%= @show.end.strftime("%a %d %b %l:%M %p") %>
</p>

<%= link_to 'Edit', edit_show_path(@show) %> |
<%= link_to 'Back', shows_path %>
