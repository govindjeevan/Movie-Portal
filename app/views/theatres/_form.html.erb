<%= form_with(model: theatre, local: true) do |form| %>
  <% if theatre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(theatre.errors.count, "error") %> prohibited this theatre from being saved:</h2>

      <ul>
      <% theatre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="row">
    <div class="form-group col-lg-6">
    <%= form.label :name %>
    <%= form.text_field :name  , class: 'form-control color'%>
  </div>
  </div>
  </div>

  <div class="field">
    <div class="row">
    <div class="form-group col-lg-6">
    <%= form.label :capacity %>
    <%= form.number_field :capacity, class: 'form-control color' %>
  </div>
  </div>
  </div>

  <div class="field">
    <div class="row">
    <div class="form-group col-lg-6">
    <%= form.label :address %>
    <%= form.text_area :address, class: 'form-control color' %>
  </div>
  </div>
  </div>

  <div class="actions">
       <%= form.submit  class:"btn btn-success btn-lg btn3d" %>
  </div>

<% end %>
